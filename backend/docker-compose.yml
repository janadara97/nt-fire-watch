services:
  db:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_USER: firewatch
      POSTGRES_PASSWORD: firewatch
      POSTGRES_DB: firewatch
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: